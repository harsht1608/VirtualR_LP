apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: virtualr-frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: virtualr.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: virtualr-frontend-service
                port:
                  number: 80
